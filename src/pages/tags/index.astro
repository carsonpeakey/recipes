---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';

const recipes = await getCollection('recipes');
const techniques = await getCollection('techniques');
const allContent = [...recipes.filter((r) => !r.id.startsWith('_')), ...techniques];

const tagCounts = new Map<string, number>();
allContent.forEach((item) =>
  item.data.tags?.forEach((tag: string) => tagCounts.set(tag, (tagCounts.get(tag) || 0) + 1))
);

const sortedTags = Array.from(tagCounts.entries()).sort((a, b) => b[1] - a[1]);
---

<BaseLayout title="Tags">
  <h1 style={`color: var(--color-sky)`}>Tags</h1>
  {sortedTags.length === 0 && <p>No tags yet.</p>}
  <div class="flex flex-wrap gap-2 mt-4">
    {sortedTags.map(([tag, count]) => (
      <a href={`/tags/${tag}/`} class="px-3 py-1 rounded-lg" style={`background: var(--color-ocean); color: var(--color-sky-light)`}>
        {tag} ({count})
      </a>
    ))}
  </div>
</BaseLayout>
